# AI 协作记录：悦食BI 自然语言查数模块

> 记录本项目中「用人 + AI 协作」完成 AI 模块的真实过程：用了哪些 AI 工具、任务怎么拆、AI 在哪里出错/有编造风险、我怎么发现并修复，以及哪些部分决定自己写。

---

## 1. 概述

悦食BI 是一个连锁餐饮（5 家门店）的经营数据看板。前后端看板（登录、筛选、KPI、ECharts 图表、鉴权）在本次会话开始前已完成，本阶段的目标是给看板接入一个 **AI 自然语言查数模块**：用户用自然语言问数据问题，AI 转成 SQL 查本地 MySQL，回答必须真实、可溯源、绝不编造。

核心约束（也是验收红线）：
1. 所有回答数据**只能来自数据库**；
2. 返回数据**必须标注来源**（哪张表、多少行）；
3. 数据库没有就**礼貌说明「数据不存在」**，禁止胡编乱造。

下文如实记录这次开发的 AI 协作方式。

---

## 2. 用了哪些 AI 工具

| 工具 | 角色 | 说明 |
|---|---|---|
| **Hermes Agent**（Nous Research） | 开发编码助手 | 负责侦察、方案、编码、自审、测试、git 等全流程，模型为 DeepSeek-V4-Pro |
| **Hermes Skills**（技能库） | 领域知识 | 已有技能直接复用，如 `langgraph-persistence`（checkpointer/Store 选型）、`git-push-safety`（推送前密钥扫描） |
| 终端 / Git / 补丁工具 | 执行层 | 命令执行、文件编辑、版本提交 |

> ⚠️ 区分两个「AI」：**开发用的 AI**（Hermes Agent，帮我写代码）与**产品内的 AI**（DeepSeek `deepseek-v4-flash` + LangGraph，是本次要构建的功能本身）。下文「AI 出错」指开发过程中的助手；「编造风险」指产品 AI 的幻觉风险。

---

## 3. 任务怎么拆（7 个阶段）

把「接入 AI 查数模块」这个模糊的大需求，拆成可验证的小步骤，每步都有明确产物：

| 阶段 | 动作 | 产物 |
|---|---|---|
| 1. 侦察 | 读 F:\11 现有代码（发现 `call_ai()` 占位钩子）+ 参考项目 AIStoreSystem 的实现 | 现状清单 + 参考架构 |
| 2. 方案对比 | 对比「MCP 工具」vs「进程内 Text2SQL」两条路线，写方案文档 | `docs/AI_MODULE_PLAN.md` |
| 3. 确认 | 关键决策（技术路线 / 模型 / 持久化）与我逐一定盘 | 决策记录 |
| 4. 实施 | 写 `ai_agent/`（schema_cache / tools / prompts / agent）+ 接入 `main.py` | 可运行代码 |
| 5. 自审 | 主动复查缺陷 / 回归 / 测试缺口（Critical/Warning/Suggestion） | 修复了 2 个真实 bug |
| 6. 验证 | 护栏测试 + 真实查库 + LLM 端到端 + **数字独立对账** | 验证结果 |
| 7. 提交 | 密钥扫描 → commit → push | 4 个提交 |

关键经验：**需求不会被一步实现，先拆成「侦察 → 方案 → 确认 → 实施 → 自审 → 验证 → 提交」，每步留痕，才能抓住 AI 在中间步骤的错。**

---

## 4. 一个真实 prompt 例子

我的原始需求（润色前）大致是：「项目已完成前后端，现在要 AI 模块，用户自然语言问数据，数据只能来自数据库、要标记来源，没有就返回数据不存在，别乱编；实现可以是 MCP 工具或 Text2SQL；用 langchain/langgraph + checkpoint 存对话」。

**润色成一条可执行的 prompt**（发给 AI 编码助手时更靠谱）：

> 为悦食BI（FastAPI + MySQL，已有 `/api/ai/chat` 占位钩子）接入自然语言查数模块。
> **功能**：① 支持自然语言询问经营数据；② 回答数据仅来自 MySQL `Demo` 库且标注数据来源（表名 + 行数）；③ 查无数据时礼貌返回「数据不存在」，严禁编造。
> **技术约束**：① 基于 LangChain/LangGraph 实现 NL2SQL；② 用 checkpointer 持久化多轮对话；③ 请对比「MCP 工具」与「进程内 Text2SQL」两条实现路线，给出推荐与理由，先出方案文档、我确认后再动手。

效果：AI 没有直接写代码，而是先侦察现状、读参考项目、产出「Text2SQL vs MCP」对比表并在文档里**明确推荐 Text2SQL（进程内工具）**——因为图表/查数都在同进程，MCP 是纯开销。**把「先出方案、再确认、后实施」写进 prompt，是控制 AI 不脱缰的关键。**

---

## 5. AI 在哪里出了错 / 有编造风险，怎么发现、怎么修

### 5.1 开发期 AI 犯的错（真实 bug）

**① 大小写匹配 bug（静默失效，最阴）**
- 现象：SQL 护栏里，把 SQL `upper()` 后再用正则匹配表名，但模式用了小写 → 永远匹配不上。
- 后果：`source.tables`（数据来源标记）恒为空；更严重的是 **`DENIED_TABLES`（`users` 等敏感表拦截）形同虚设**。
- 为什么难发现：不报错、测试全绿，是「静默失效」。
- 发现方式：我在**自审阶段**跑冒烟脚本，看到「来源为空」异常，追到正则；再用「只读护栏」补测了 `SELECT password_hash FROM users`，确认拦截失效。
- 修复：模式统一 `t.upper()`；并把该教训写进 skill。

**② 敏感表泄露（数据安全隐患）**
- 现象：`get_schema` 用 `information_schema.COLUMNS` 把 `users`（含口令哈希）等所有表 dump 给了模型，模型理论上能查口令。
- 发现方式：自审时意识到「schema 暴露」≠「只读护栏」两层都要堵——只藏 schema，模型仍能拼 `SELECT password_hash FROM users`。
- 修复：**两层**——schema 缓存只返回业务表白名单 + 护栏加表级拒绝。

**③ 小坑**：补丁工具把一段代码缩进打乱（触发 `IndentationError`，被语法检查当场抓住）；curl 写 MSYS 路径失败、`AsyncSqliteSaver` 是 async 上下文管理器而非实例等，均已沉淀进 `langgraph-text2sql` skill，避免二次踩坑。

### 5.2 「编造数字」的风险：防堵 + 独立对账

这是本项目的红线。我不只写提示词约束，而是做了**机制性防堵**：

- **图表数据不让模型传值**：`render_chart` 工具由服务端**重跑 SQL** 生成图表数据，而不是信任模型从上下文里抄数字——从根上断掉「图表数字是编的」的可能。
- **只读护栏**：写操作 / 注入 / 多语句 / 敏感表四重拦截，模型只能读、不能改。
- **独立对账验证**：对 AI 的每个回答，把它的口径还原成 SQL **直查数据库复核**，逐位比对。

一次「虚惊」很能说明问题：AI 给「各门店营业额」柱状图的值是 `[89569, 85512, 85037, 84252, 83529]`，我第一眼怀疑它把「品类」当成了「门店」（复用了我之前问品类的答案）。独立核算后发现**没错**：本库 5 个门店每个恰好对应唯一品类（日料=Super Tetsudo 等 1:1），门店排行与品类排行数值天然一致，且五者之和 = 427,899，正好等于看板全量总营业额。**怀疑 → 独立复算 → 证实无编造**，这就是保质流程。

---

## 6. 哪些部分自己写，为什么

| 部分 | 谁写 | 为什么 |
|---|---|---|
| 看板前后端（登录动画、ECharts 图表、筛选交互、鉴权、聚合 API） | **我（作者）** | 会话前已完成；属于确定性、可预判的 UI/接口逻辑，手写更可控、易维护 |
| AI 模块的设计与关键决策（Text2SQL vs MCP、选 DeepSeek、`.env` 外部化、安全设计） | **我拍板，AI 给方案** | 架构取舍与安全边界必须人负最终责任 |
| AI 模块的编码实现（LangGraph Agent、护栏、SSE、图表工具） | **AI 主写，我复核** | LangGraph/SSE/护栏属较新领域，AI 填空效率高；但我要求「先方案后实施 + 自审 + 独立对账」来兜底 |

结论：**AI 适合在明确框架内「填空」+ 需要反复验证的新领域；人负责拆任务、做架构决策、把安全关、做最终验收。** 分工的边界是「谁对结果负责」。

---

## 7. 经验总结

1. **Prompt 里写「先出方案、确认后再动手」**，能把 AI 从「直接写代码」拨到「可评审的设计」。
2. **AI 的静默失效比报错更危险**——必须靠自审 + 针对性的负面测试（如注入、空数据、越界）兜住。
3. **数字可信不能靠「AI 说它对」**，要靠机制（服务端重跑 + 只读护栏）和独立对账。
4. **把踩过的坑沉淀成 skill**（`langgraph-text2sql`），下次同类型任务直接复用，不重复付学费。