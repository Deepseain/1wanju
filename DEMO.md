# AI 助手演示（DEMO）

> 三个真实问题，展示悦食BI AI 助手怎么查数、怎么出图、怎么在没数据时诚实兜底——以及为什么这些数字可信。

演示环境：`http://localhost:5000`，账号 `admin / admin123`，看板右下角「AI 悬浮球」打开对话抽屉。

---

## 问题一：门店排行查数

**问**：「上周哪家门店营业额最高？」

**AI 回答（实录）**：

```
上周（2026-07-24 至 2026-07-30）营业额最高的门店是 Super Tetsudo，营业额为 8,069.00 元。

各门店上周营业额排行如下：
1. Super Tetsudo：8,069.00 元
2. Juicy Bao Bao：8,003.00 元
3. Super Souper：7,068.00 元
4. Makai Poke：6,983.00 元
5. Arigato Sando：6,882.00 元

口径说明：以数据库中销售日期最大值 2026-07-31 为「今天」，向前推算 7 天作为上周区间；营业额 = 销售额合计。
（数据来源：v_sales_detail 表，共 5 行）
```

**为什么可信**：
- 末尾明确标注了来源表与行数；
- 可独立复现——把口径还原成 SQL 直查库，逐位对账：

```sql
SELECT store_name, SUM(amount)
FROM v_sales_detail
WHERE date BETWEEN '2026-07-24' AND '2026-07-30'
GROUP BY store_name ORDER BY SUM(amount) DESC;
```

对账结果与 AI 回答**完全一致**（8069 / 8003 / 7068 / 6983 / 6882），零编造。

---

## 问题二：图表生成

**问**：「用柱状图展示各门店的营业额对比」

**AI 回答（实录）**：生成了一张交互式柱状图，并附文字排行：

```
1. Super Tetsudo：89,569.00 元
2. Juicy Bao Bao：85,512.00 元
3. Makai Poke：85,037.00 元
4. Arigato Sando：84,252.00 元
5. Super Souper：83,529.00 元
（图表：柱状图，全量区间 2026-05-01 至 2026-07-31）
```

**为什么可信**：
- 图表数据由 `render_chart` 工具**服务端重跑 SQL** 产生，模型不直接传值，断掉「图表数字是编的」的可能；
- 独立验证 ①：五个门店全量营业额之和 = 89,569 + 85,512 + 85,037 + 84,252 + 83,529 = **427,899**，正好等于看板 KPI「总营业额」；
- 独立验证 ②：本库 5 个门店每个对应唯一品类（日料=Super Tetsudo、点心=Juicy Bao Bao…，1:1 映射），所以「门店排行」与「品类排行」数值天然一致——这正是数据自洽的旁证。

---

## 问题三：无数据兜底（禁编造）

**问**：「数据库里有没有新加坡门店的数据？」

**AI 回答（实录）**：

```
抱歉，数据库中没有查到「新加坡」门店的数据。

我按门店名称和区域两个字段都查过「新加坡」关键字，结果为 0 条记录。
目前库里的门店共 5 家，全部位于上海：Super Souper / Makai Poke / Juicy Bao Bao / Arigato Sando / Super Tetsudo。

所以暂无新加坡门店的销售数据可查询，如需了解其他区域或门店，欢迎告诉我。
```

**为什么可信**：
- 面对「结果为空」，AI 选择了**如实说明**而不是编一个门店出来；
- 它同时给出了真实的 5 家门店清单（来自 `stores` 表），说明它确实查了库、而非空口推脱。

> 补充：越界问题同样诚实——问「帮我查员工工资」，AI 明确回复「库内没有员工/工资数据表，不能凭空编造数字」。

---

## 数字为什么可信：四点机制

1. **数据只来自数据库**：所有回答经只读 SQL 查询本地 MySQL，无任何外部知识「补数」；
2. **只读护栏**：写操作 / 注入 / 多语句 / 敏感表四重拦截，AI 只能读、不能改；
3. **来源标记**：每个回答末尾标注（数据来源：xxx 表，共 N 行）；
4. **独立对账**：上文的三个问题都做了「口径还原成 SQL 直查库」复核，数字逐位一致。

一句话概括：**这套 AI 的可信，不靠「相信它说对了」，靠「每一个数字都能被一条 SQL 复现」。**