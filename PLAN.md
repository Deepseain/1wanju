# 悦食BI · 门店经营数据看板 — 开发计划

> 版本 v1.0 | 2026-08-19
> 数据源：本机 MySQL `Demo` 库（sales / stores / products / v_sales_detail，已完成清洗）

---

## 1. 项目概述

连锁餐饮公司（5 家门店）的经营数据看板：**FastAPI 异步后端 + 浅色商务风前端**，
支持按日期区间查看每日营业额 / 订单数 / 客单价，Top 10 商品分析，
并**预留 AI 对话模块**（本期占位，后续接入大模型做自然语言查数与经营洞察）。

## 2. 已确认决策

| 决策项 | 结论 |
|---|---|
| 看板风格 | 浅色商务风（白底卡片 + 紫色强调，与登录页视觉统一） |
| 后端框架 | FastAPI（异步，为 AI 模块与并发查询留优化空间） |
| 前端方案 | Jinja2 模板 + 原生 JS + ECharts（零构建） |
| 扩展范围 | 支付方式占比 + 门店排行 + 品类结构，全部纳入本期 |
| 运行环境 | `E:\Anacoda\envs\agentLang`（Python 3.13，依赖已齐备） |

## 3. 设计规范

### 3.1 色彩体系（延续 CareerCompass 登录页基因）

| 用途 | 色值 |
|---|---|
| 主强调色（按钮/聚焦/选中） | `#5B21B6` 紫 |
| 标题 / 深色底 | `#1a1a2e` 深夜蓝 |
| 页面背景 | `#F6F6F9` |
| 卡片背景 | `#FFFFFF`，0.5px 边框 `#E4E4EC` |
| 数据图主色带 | `#5B21B6 → #7C5CE0 → #A78BFA → #C4B5FD`（紫色系渐变） |
| 辅助对比色 | `#F59E0B` 琥珀（客单价线） |
| 环比上涨 | 红 `#DC2626`（涨红） |
| 环比下跌 | 绿 `#16A34A`（跌绿） |

### 3.2 页面结构

- **登录页 `/`**：复刻参考页——左侧渐变面板 + 四个几何小人动画
  （眼睛跟随鼠标、输入时互相看、输密码集体扭头回避、密码可见偷看、
  登录失败集体摇头 + 橙小人生气嘴），右侧极简表单，接入真实登录接口。
- **看板页 `/dashboard`**：
  - 顶栏：品牌 Logo + 日期区间筛选器（起止日期 + 近7天/近30天/全部快捷档）+ 用户菜单（退出）
  - KPI 卡 ×4：总营业额 / 订单数 / 客单价 / 销售件数，各带环比箭头
  - 每日营业额趋势：营业额柱 + 客单价折线组合图（ECharts，hover 联动）
  - 支付方式占比：环形图（中心显示总订单数）
  - Top 10 畅销商品表：排名徽章 / 商品 / 品类 / 销量 / 销售额 / 占比
  - 门店排行：横向条形图（含品类与区域标签）
  - 品类结构：主食/点心/小食/饮料 占比
  - 右下角 **AI 助手悬浮球** → 展开聊天抽屉（预留）

### 3.3 交互细节

- 日期筛选变更即重新拉取全部数据（单接口一次返回）
- 无数据日期区间给空态提示，不报错
- 金额千分位格式化，环比保留 1 位小数
- 响应式：≤900px 隐藏登录页左侧面板；看板栅格自动折叠

## 4. 技术架构

```
浏览器（登录页 / 看板页，ECharts 渲染）
   │  fetch + Bearer Token
   ▼
FastAPI (uvicorn, 端口 5000)
   │  aiomysql 异步连接池
   ▼
MySQL Demo 库
   ├─ stores / products（维表）
   ├─ sales（清洗后 11,990 行，强类型 + 索引）
   ├─ sales_raw / sales_rejects（留痕）
   └─ v_sales_detail（三表 JOIN 视图，API 直接聚合）
```

- **鉴权**：`users` 表（首次启动自动建表并写入演示账号），SHA-256 口令；
  登录成功发 token（内存会话，有效期 24h，重启失效），`/api/*` 除登录外均需 Bearer Token。
- **异步**：aiomysql 连接池（min 1 / max 5），所有查询走 `async def`，
  后续 AI 模块并发调用不会阻塞数据接口。
- **AI 预留**：`/api/ai/chat` 已定义请求/响应结构（messages 数组），
  当前返回占位话术；预留 `call_ai()` 钩子函数，接入大模型只改这一处；
  环境自带 `sse-starlette`，届时可平滑升级为流式输出。

## 5. API 设计

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/login` | `{username, password}` → `{token, username}` |
| POST | `/api/logout` | 注销 token |
| GET | `/api/me` | 校验 token，返回当前用户 |
| GET | `/api/dashboard?start=&end=` | 看板主数据（见下方结构） |
| POST | `/api/ai/chat` | `{messages:[{role,content}]}` → `{reply}`（占位） |

`/api/dashboard` 返回结构：

```json
{
  "meta": { "min_date": "2026-05-01", "max_date": "2026-07-31",
            "start": "...", "end": "..." },
  "kpi":  { "revenue": 427899.0, "orders": 11990, "aov": 35.68, "items": 14203,
            "compare": { "revenue_pct": 12.4, "orders_pct": 8.1, ... } },
  "daily":   [ { "date": "2026-05-01", "revenue": 5120.0,
                 "orders": 130, "aov": 39.38 } ],
  "top_products": [ { "rank": 1, "name": "豚骨拉面", "category": "主食",
                      "qty": 1194, "revenue": 38208.0, "share": 8.9 } ],
  "stores":  [ { "store_id": "S01", "name": "Super Souper", "category": "拉面",
                 "district": "上海·徐汇", "revenue": 98000.0, "orders": 2400 } ],
  "payments": [ { "name": "支付宝", "value": 2452 } ],
  "categories": [ { "name": "主食", "value": 210000.0 } ]
}
```

环比（compare）口径：所选区间与其等长前一区间对比；区间起点为数据最早日期时无环比。

## 6. 功能清单

**本期核心**
1. 登录页（动画小人完整复刻 + 真实鉴权，演示账号 `admin / admin123`）
2. 日期区间筛选 + 快捷档（近7天 / 近30天 / 全部）+ 数据边界自动约束
3. KPI 四卡（总营业额 / 订单数 / 客单价 / 销售件数 + 环比）
4. 每日营业额趋势组合图（柱 + 线，hover 联动）
5. Top 10 商品表
6. AI 对话框（前端组件完整，后端占位 + 钩子）

**本期扩展**
7. 支付方式占比环图、门店排行、品类结构
8. 退出登录、token 过期自动跳回登录页

**后续规划（AI 模块）**
- 自然语言查数：对话中问"上周哪家店营业额最差"，AI 调聚合接口/生成 SQL 返回图表
- 自动洞察：环比异常（如客单价连续下滑）主动生成分析文案
- 经营日报：按日自动汇总生成文本报告
- 流式输出：sse-starlette 逐 token 推送

## 7. 目录结构

```
F:\11\app\
├─ main.py               # FastAPI 应用（路由 + 鉴权 + 查询）
├─ requirements.txt
├─ static\
│  └─ echarts.min.js     # 本地化 ECharts（离线可用）
└─ templates\
   ├─ login.html         # 登录页（动画小人）
   └─ dashboard.html     # 数据看板
```

## 8. 里程碑

| # | 内容 | 状态 |
|---|---|---|
| M1 | 计划文档 + 环境确认（agentLang） | ✅ |
| M2 | FastAPI 后端（鉴权 + dashboard 聚合 API + AI 占位） | ⏳ |
| M3 | 登录页（复刻动画 + 接口对接） | ⏳ |
| M4 | 看板前端（筛选/KPI/图表/Top10/AI 抽屉） | ⏳ |
| M5 | 启动验证（接口 curl 测试 + 浏览器预览） | ⏳ |
| M6 | AI 模块接入（后续迭代） | 待启动 |
